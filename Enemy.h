// Enemy.h
//
//
/*
#ifndef ENEMY_H

#define ENEMY_H

#include "Character.h"

struct Goblin{
    attack(int dmg, int critCh, int critDm);
};

struct Orc{
    Character::baseStats{200, 30, 10, 5, 25};
};

struct Skeleton{
    Character::baseStats{50, 25, 1, 25, 35};
};

class Enemy (type) : public Character{
    public:

        enum struct EnemyType {
            goblin,
            orc,
            skeleton,
            count,
        };

        Enemy(EnemyType type, int hp, int dmg, int def, int critC, int critDmg) : Character(hp, dmg, def, critC, critDmg);



        Enemy::~Enemy();

        EnemyType Enemy::getRandomType();
        EnemyType Enemy:getType() const;

        Enemy Enemy::makeRandomEnemy()


    private;
        EnemyType type;

}

#endif

 */
